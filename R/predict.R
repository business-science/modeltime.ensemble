# #' Predict from a Modeltime Ensemble
# #'
# #' @param object A Modeltime Ensemble
# #'
# #' @export
# predict.mdl_time_ensemble <- function(object, new_data, ...) {
#
#     if (inherits(object, "mdl_time_ensemble_avg")) {
#
#     } else if (inherits(object, "mdl_time_ensemble_stack")) {
#
#     } else {
#         rlang::abort("No method for this modeltime ensemble class.")
#     }
#
# }
#
# predict_ensemble_average <- function(object, new_data, ...) {
#
# }
